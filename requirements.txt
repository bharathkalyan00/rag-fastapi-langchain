# --- Web & API Framework ---
fastapi==0.111.0
uvicorn==0.30.1
langserve[all]==0.2.2
python-dotenv==1.0.1
requests==2.32.3

# --- LangChain Core ---
langchain==0.2.1
langchain-community==0.2.1
langchain-core==0.2.1

# --- Vector Store & Processing ---
# Using the standard chromadb and pypdf for document handling
chromadb==0.5.0
pypdf==4.2.0
langchain-chroma==0.1.1
langchain-text-splitters==0.2.0

# --- Third Party Providers (API Based) ---
# These are lightweight because the heavy lifting happens on their servers
langchain-groq==0.1.3
langchain-google-genai==1.0.5

# --- Frontend (If you deploy client.py separately) ---
streamlit==1.35.0